/*
 * File for commonly used backend functions
 */

import { SESSION_EXPIRED } from './constants';

//  Maps back-end errors to error constants to return
export const handleErrorResponse = err => {
  // if (err === ) return SESSION_EXPIRED;
};
