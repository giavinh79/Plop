import React from 'react'
import { layout, subheader } from '../../globalStyles'

export default function Schedule() {
    return (
        <div style={layout}>
            <p style={subheader}>To Be Done</p>
        </div>
    );
}